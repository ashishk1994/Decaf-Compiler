class Progra {
	int main () {
		int a, b, x, y, z;
		a = 10;
		b = 20;
		x = a;
		y = b;
		z = gcd(x, y);
	}
	int gcd(int a, int b) {
		if (b == 0) { return(a); }
		else { return( gcd(b, a % b));}
	}
}
